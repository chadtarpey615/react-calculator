import React from 'react'

const Display = ({ number }) => {
    console.log(number)
    return (
        <div>
            <input type="text" readOnly />
        </div>
    )
}

export default Display
