import React, { useState } from 'react'

const Button = ({ value, number }) => {

    return (
        <>

            <button>
                {value}
            </button>
        </>

    )
}

export default Button
